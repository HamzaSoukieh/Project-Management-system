// const express = require("express");
// const router = express.Router();
// const projectCtrl = require("../controllers/trackingController");
// const auth = require("../middleware/is_auth");
// const canViewProject = require("../middleware/canViewProject"); // we'll create this

// router.get("/:id/tracking", auth, canViewProject, projectCtrl.getTracking);

// module.exports = router;
